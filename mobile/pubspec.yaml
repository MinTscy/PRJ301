name: lucy_mobile
description: LUCY LMS mobile Pro dashboard for live-room learner management.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  just_audio: ^0.10.6
  permission_handler: ^12.0.3
  socket_io_client: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
